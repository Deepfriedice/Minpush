Example of using several different types of literals in Minpush.

{ start :
	'Hello"             (add "Hello" to array)
    h20H oA             (add a space to array)
	[57 6f 72 6c 64]    (add "World" to array)
    `! oA               (add an exclamation mark to array)
	d0D d12D _          (print first 12 bytes of array)
    h0aH .              (print newline char)
: stop }
